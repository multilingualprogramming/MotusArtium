query ArtworksByArtist($artistId: String!, $languageCode: LanguageCode!) {
  searchItems(
    query: {
      and: [
        { property: "P170", value: $artistId }
        { property: "P31", value: "Q3305213" }
      ]
    }
  ) {
    edges {
      node {
        id
        artworkLabel: label(languageCode: $languageCode)
      }
    }
  }
}
