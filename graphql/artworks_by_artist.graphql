query ArtworksByArtist($artistId: String!, $languageCode: LanguageCode!, $first: Int = 28, $after: String) {
  searchItems(
    query: {
      and: [
        { property: "P170", value: $artistId }
        { property: "P31", value: "Q3305213" }
      ]
    }
    first: $first
    after: $after
  ) {
    edges {
      node {
        id
        artworkLabel: label(languageCode: $languageCode)
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
