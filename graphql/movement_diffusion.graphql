query MovementDiffusion($id: ItemId!, $languageCode: LanguageCode!) {
  item(id: $id) {
    id
    movementLabel: label(languageCode: $languageCode)
    country: statements(propertyId: "P17") {
      value {
        ... on ItemValue {
          id
          label(languageCode: $languageCode)
        }
      }
    }
  }
}
