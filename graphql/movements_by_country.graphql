query MovementsByCountry($countryId: String!, $languageCode: LanguageCode!, $first: Int = 20, $after: String) {
  searchItems(
    query: {
      and: [
        { property: "P31", value: "Q968159" }
        { property: "P17", value: $countryId }
      ]
    }
    first: $first
    after: $after
  ) {
    edges {
      node {
        id
        movementLabel: label(languageCode: $languageCode)
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
