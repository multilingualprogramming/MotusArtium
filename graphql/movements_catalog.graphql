query MovementsCatalog($languageCode: LanguageCode!) {
  searchItems(
    query: {
      property: "P31"
      value: "Q968159"
    }
  ) {
    edges {
      node {
        id
        movementLabel: label(languageCode: $languageCode)
      }
    }
  }
}
